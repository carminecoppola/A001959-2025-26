<resources>

    <!--
        Application theme used by MainActivity.
        A NoActionBar theme keeps the UI focused on the permission demonstration.
    -->
    <style name="Theme.L19SecurityModelDemo" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>

</resources>