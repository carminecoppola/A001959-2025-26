<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        CAMERA is classified as a dangerous permission.
        Therefore, it must be declared here and also requested at runtime.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.L19SecurityModelDemo">

        <!--
            MainActivity is exported because it is the launcher Activity.
            External apps can start it only through the launcher intent.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!--
                This intent-filter marks MainActivity as the entry point
                of the application.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>