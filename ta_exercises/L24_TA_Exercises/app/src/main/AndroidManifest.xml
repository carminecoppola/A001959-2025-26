<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        This demo does not require INTERNET permission.
        Network permissions should be declared only when strictly necessary.
    -->

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.L24SecureReleaseChecklist">

        <!--
            This Activity is exported because it is the launcher Activity.
            Other app components should use android:exported="false" by default.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>