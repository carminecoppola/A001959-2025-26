<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout for a single RecyclerView row.
    This file is used by ItemStudentBinding inside StudentViewHolder.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/itemStudentContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!--
        Primary text of the row.
    -->
    <TextView
        android:id="@+id/tvStudentName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Student name"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!--
        Secondary text of the row.
    -->
    <TextView
        android:id="@+id/tvStudentCourse"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Course"
        android:textSize="14sp"
        android:layout_marginTop="4dp" />

</LinearLayout>