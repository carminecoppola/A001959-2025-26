<resources>

    <!--
        Simple application theme without ActionBar.
        This keeps the screen focused on the RecyclerView demonstration.
    -->
    <style name="Theme.L13RecyclerViewDemo" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>

</resources>