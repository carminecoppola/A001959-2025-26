<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.L16SharedPreferencesDemo">

        <!--
            MainActivity is the launcher Activity.
            This app demonstrates local key-value settings storage.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!--
                Launcher intent-filter: this Activity is the entry point of the app.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>