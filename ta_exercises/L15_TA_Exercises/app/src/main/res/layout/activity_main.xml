<?xml version="1.0" encoding="utf-8"?>
<!--
    Main screen for the Room Database demo.
    The Activity observes database data and renders it through RecyclerView.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!--
        Title of the lesson demo.
    -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Room Database Demo"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_marginBottom="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/etItemTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!--
        User input for creating a new Room entity.
    -->
    <EditText
        android:id="@+id/etItemTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Enter item title"
        android:inputType="text"
        android:singleLine="true"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintBottom_toTopOf="@id/btnAdd"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!--
        Inserts an Item into the Room database.
    -->
    <Button
        android:id="@+id/btnAdd"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Add Item"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/etItemTitle"
        app:layout_constraintBottom_toTopOf="@id/btnClearCompleted"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!--
        Demonstrates an additional DAO query.
    -->
    <Button
        android:id="@+id/btnClearCompleted"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Clear Completed"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/btnAdd"
        app:layout_constraintBottom_toTopOf="@id/tvEmptyState"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!--
        Shows simple feedback about database content.
    -->
    <TextView
        android:id="@+id/tvEmptyState"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="No items stored in Room yet."
        android:textSize="16sp"
        android:textAlignment="center"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/btnClearCompleted"
        app:layout_constraintBottom_toTopOf="@id/recyclerViewItems"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!--
        RecyclerView displays items loaded from the Room database.
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewItems"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/tvEmptyState"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>